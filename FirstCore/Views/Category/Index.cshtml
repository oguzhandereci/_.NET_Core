@model List<Category>
@{
    ViewData["Title"] = "Index";
}
<a asp-action="Add" asp-controller="Category" class="btn btn-info">Ekle</a>
<h3>@TempData["message"]</h3>
<div class="container">
    <div class="row">
        <div class="col-md-6 col-md-offset-3">
            <table class="table">
                <thead>
                <tr>
                    <th>#</th>
                    <th>Kategori Adı</th>
                    <th>Ürün Sayısı</th>
                    <th>İşlemler</th>
                </tr>
                </thead>
                <tbody>
                @for (int i = 0; i < Model.Count; i++)
                {
                    var item = Model[i];
                    <tr>
                        <td scope="row">@(i+1)</td>
                        <td>@item.CategoryName</td>
                        <td>@item.Products.Count</td>
                        <td>
                            <a asp-action="Edit" asp-controller="Category" asp-route-id="@item.Id" class="btn btn-info">Düzenle</a>
                            <a asp-action="Delete" asp-controller="Category" asp-route-id="@item.Id" class="btn btn-danger">Sil</a>
                        </td>
                    </tr>
                }
                
                </tbody>
            </table>
        </div>
    </div>
</div>






